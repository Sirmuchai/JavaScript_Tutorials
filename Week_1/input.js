


//prompt accepts input where as parseInt convert the input to an integer
let a = parseInt(prompt("Enter your number:"));

function square(x){
    return Math.sqrt(x);
}

console.log(`The sqare of ${a} is: ${square(a)}`)